apiVersion: v1
kind: Pod
metadata: 
  name: pod-config
spec:
  containers:
  - name: nginx
    image: nginx
    envFrom:
    - configMapRef:
        name: neelareddy25
    # env:
    # - name: course
    #   valueFrom:
    #     configMapKeyRef:
    #       name: neelareddy25
    #       key: course
    # - name: trainer
    #   valueFrom:
    #     configMapKeyRef:
    #       name: neelareddy25
    #       key: trainer